import React, {useState, useEffect, useRef, useReducer, Fragment} from 'react';
import {} from 'react-icons/fa';
import {useHistory, useLocation} from 'react-router-dom';
import PropTypes from 'prop-types'

const AlgorithmsWorks = (props) => { // Component that is rendered on the Algorithm page that describes how these algorithms work
    return (
        <Fragment>

        </Fragment>
    )
}

AlgorithmsWorks.defaultProps = {
    
}

AlgorithmsWorks.propTypes = {
    
}

export default AlgorithmsWorks